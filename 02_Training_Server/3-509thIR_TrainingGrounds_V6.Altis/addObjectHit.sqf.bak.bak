_targetString = _this select 0;
_targetObject = _this select 1;
_alreadyAdded = _targetObject in objectsHit;
if (!_alreadyAdded) then
{
	objectsHit = objectsHit + [_targetObject];
	_targetObject setPos (getPos pos);
};